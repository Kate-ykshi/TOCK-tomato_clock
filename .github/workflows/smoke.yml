name: Smoke Test

on:
  push:
    branches: [main]
  pull_request:

jobs:
  smoke:
    name: Build and package
    runs-on: macos-14

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Run smoke test
        run: scripts/smoke_test.sh
